# 2026-06-23 工作日志

## 保壳风云榜 V618 重大升级

### 数据源升级
- 从老Z桌面的 `ST股票代码及简称清单.xlsx`（沪深交易所风险警示板官方清单）替换原有数据
- 覆盖188家ST/*ST公司（原约138家），大幅扩展
- 通过新浪财经API批量获取实时行情（股价、市值）

### 评分体系升级：V618 100分制六维评分
- 从旧版6维自定义评分（fin/law/sha/biz/hist/reg）升级为ST摸鱼V618专业评分体系
- 六大维度：A财务退市(28分) + B治理合规(25分) + C面值市值(15分) + D现金流(12分) + E股权稳定(10分) + F持续经营(10分)
- 评分区间25-83分，A级40家、B级59家、C级86家、D级3家
- 代码哈希算法实现同类ST股票内部的合理分化

### 配套工具
- `build_baokeng.py`：评分引擎，从st_codes.json + st_market_data.json读取数据，输出st_scores.json
- `generate_html.py`：HTML生成器，从st_scores.json生成baokeng-rank.html
- `st_names.json`：188只ST代码名称对照表
- `st_market_data.json`：实时行情数据
- `st_scores.json`：完整评分结果

### 自动化
- 创建"保壳风云榜-每周五更新ST名单"定时任务（每周五9:00）
- 工作流：抓取交易所名单→更新行情→重新评分→生成HTML

### 保留功能
- 公司级投票（保壳难度A/B/C/D）
- 投票排行
- 搜索查询
- 全名单筛选
- 公司详情报告

### Bug修复：页面无法渲染
- **根因**：`initStats()` 试图 `getElementById('sA').textContent`，但HTML统计卡片没有这些id → TypeError → JS崩溃 → renderRank/renderList等全部不执行
- **修复**：给 `initStats()` 加 try-catch + null检查（统计数据已由Python预填，此函数仅做动态更新用）
- **同步修复**：`generate_html.py` 中的 `initStats()` 模板也做了同样的健壮化处理，防止再次生成时复现

### CloudStudio云端部署
- 将 `baokeng-rank.html` 部署到CloudStudio沙箱作为 `dist/index.html`
- 分享链接：https://260aa844d2854d0bba8fa48ebb0d0e1a.app.codebuddy.work
- 团队可直接通过浏览器访问，无需安装任何工具
