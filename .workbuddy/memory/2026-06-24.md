# 2026-06-24 工作日志

## GitHub Pages 部署成功
- 用户名：Zsheng007
- 仓库：https://github.com/Zsheng007/baokeng-ranking
- Pages 永久链接：https://zsheng007.github.io/baokeng-ranking/
- 部署方式：git push main 分支 + API 启用 Pages
- 部署方式：git push main + GitHub Actions workflow (pages.yml) 自动部署
- 注意：build_type 初始设为 workflow 需要 Actions，已添加 .github/workflows/pages.yml
- Actions 工作流已运行成功 (completed/success)

## 路径回顾
- Gitee：需要实名认证才能用 Pages，用户未完成
- CloudStudio sandbox：链接需登录态，外部无法访问
- localtunnel/surge/serveo：沙盒环境受限
- GitHub Pages：经典 token (ghp_) + repo 权限 + GitHub Actions 工作流，部署成功

## Git 仓库配置
- Remote: https://github.com/Zsheng007/baokeng-ranking.git
- 分支: main (包含 index.html + .github/workflows/pages.yml)
- 更新流程: 修改 index.html → git commit → git push → Actions 自动部署

## 企业微信群推送
- Webhook: a7c988c1...（announcement-filter 技能内置）
- 推送内容1：Markdown 介绍消息 + Pages 链接
- 推送内容2：HTML 文件附件（下载后浏览器打开即可离线使用）
- 状态：两次推送均成功 (errcode: 0)

## V618 评分体系反转（2026-06-24 晚间）
### 变更内容
- **评分方向**：从"分数越高=风险越大" 反转为 "分数越高=保壳能力越强"
- **算法**：新分 = 100 - 旧分，各维度也对应反转（A1=5-A1_old 等）
- **新评级阈值**：A>65, B:46-65, C:26-45, D<=25（分布不变：A=40 B=59 C=86 D=3）
- **新分数范围**：17分(*ST康佳B) ~ 75分(ST沈化)
- **涉及文件**：build_baokeng.py, st_scores.json, generate_html.py, baokeng-rank.html

### 页面文案全面更新
- 评分体系标题："保壳能力分"
- 统计卡片：A级·保壳能力强(>65) / D级·退市警钟(<=25)
- 筛选标签：A级>65 / B级46-65 / C级26-45 / D级<=25
- 详情报告：维度标签改为正向（财务健康/合规治理等）
- 排行榜：easy取前10(高分段)、hard取后10(低分段)

### 新增免责声明
- 页面底部灰色卡片，4条免责条款
- 声明"不构成投资建议"、"数据来源公开"、"不代表机构观点"

### GitHub同步
- 全部变更已 git commit + push 到 main 分支
- Actions 自动部署 (completed/success)
- 企微群已推送更新通知 + 新版文件

## 市值列新增（2026-06-24 晚间）
### 需求
- 全名单表增加「市值(亿)」列
- TOP10 最容易/最困难 每条也显示市值
- 详情页 info-row 加「总市值(亿)」芯片

### 实现方案
- `st_scores.json` 新增 `market_cap_yi` 字段（浮点数，单位亿）
- `baokeng-rank.html`：
  - 全名单表头+body加市值列
  - `COS`映射加 `market_cap_yi` + `market_cap_str`
  - `renderList()` / `showReport()` / `rankItemEasy/Hard` 均显示市值
  - 页面加载后自动 `fetch('./market_cap.json')` 获取真实市值并刷新
- `market_cap.json` 格式：`{"600053":52.3, "000001":120.5, ...}`
- `fetch_market_cap.py`：本地运行，逐只查东方财富API，写 `market_cap.json` + 更新 `st_scores.json`

### 当前状态
- ✅ HTML已更新（市值列就位，占位值"—"）
- ✅ `market_cap.json` 已创建（188只，占位0）
- ✅ `fetch_market_cap.py` 已更新（写 `market_cap.json` 格式）
- ⚠️ 沙盒无法 push 到 GitHub（port 443被封），commit e7bd8ea 在本地
- ⚠️ 真实市值数据需在本地运行 `fetch_market_cap.py` 获取

### 修复：JSONP浏览器端直接拉市值（2026-06-24 23:08）
**问题**：沙盒网络封禁东方财富API，fetch_market_cap.py 全部188只失败（Remote end closed connection）
**方案**：改用 JSONP（script标签注入），浏览器页面加载时直接向东方财富批量拉取
- 188只分4批（每批50），并行JSONP请求
- API: `push2.eastmoney.com/api/qt/ulist.np/get?secids=...&cb=callback`
- 绕过CORS，不依赖服务器端网络
- 页面打开后自动加载，约2-3秒完成
- commit `e73bf65` 已推送到 GitHub，Actions部署中

### JSONP修复第二版 (c3aca2a, 23:14)
- **Bug修复**：`Math.round(mv/100)` → `Number((mv/10000).toFixed(2))`（万元→亿元正确换算）
- **API更换**：`ulist.np/get` → `clist/get`（已验证支持`cb` JSONP回调）
- **一次拉取**：pz=6000全A股一页拉完，浏览器端filter 188只
- **超时处理**：15秒timeout + 错误日志

### 市值列位置调整 (0c1c037, 23:19)
- 用户要求市值列放在「风险原因」和「保壳分」之间
- 表头: 风险原因 → **市值(亿)** → 保壳分 → 等级 → 详情
- renderList() 数据行同步调整

### 后续步骤（用户本地执行）— 保留备选方案
1. Ctrl+F5 强制刷新 https://zsheng007.github.io/baokeng-ranking/
2. 如果还是不显示，F12控制台看 `[市值]` 日志

### 市值+昨收补全最终方案 (ad804ab, 23:28)
**最终方案**：放弃浏览器JSONP，改用服务器端腾讯财经API直接拉取。
- 新建 `fetch_all_data.py`：腾讯财经 `qt.gtimg.cn/q=shXXX,szXXX` 接口
  - 字段[3]=现价, [4]=昨收, [45]=总市值(亿)
  - 188只分5批，全部成功
- 输出 `st_market_data.json`（新增 prev_close + market_cap_yi 字段）
- `build_baokeng.py` 从 mkt 数据读取 market_cap_yi/prev_close 传递到 scores
- `generate_html.py` RAW数组末尾加 `prev_close` 字段（索引19）
- HTML COS映射加 `prev_close`，详情页 info-row 加「昨收」芯片
- 市值列从占位"—"变成真实数据（直接嵌入RAW，无需浏览器异步加载）
- 移除前端JSONP回退脚本（不再需要）

**数据流向**：
腾讯API → fetch_all_data.py → st_market_data.json → build_baokeng.py → st_scores.json → generate_html.py → baokeng-rank.html

**验证**：188/188 市值>0, 188/188 有昨收
